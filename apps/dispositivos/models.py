"""
dispositivo — PENDIENTE (solo PK por ahora). Tabla y modulo por detallar.
Fuente: docs/rondas_schema.dbml.
"""
from django.db import models


class Dispositivo(models.Model):
    """POR DETALLAR (tabla y modulo). Solo PK por ahora."""

    class Meta:
        db_table = "dispositivo"
